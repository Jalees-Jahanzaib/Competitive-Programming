#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {
    freopen("aplusbb.in", "r", stdin);
    freopen("aplusbb.out", "w", stdout);

    ll a, b;
    cin >> a >> b;
    cout << a + (b*b) << "\n";
}
